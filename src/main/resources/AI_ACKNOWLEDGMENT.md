# Acknowledgement
The logback.xml file in this directory has been generated by
ChatGPT (thinking 5.2 model), and then has been adapted to work in the
current application context by Joseph Taylor (ed20jwt@leeds.ac.uk).

Evidence for all the conversations had with the GenAI and the initial code 
presented before editing can be found in the chat exports below.

# ChatGPT Prompt:
Create me a logback config file for my spring boot app - I want the logs to contain the class and the function that they were called from.

# ChatGPT Response:
Here’s a clean **`logback-spring.xml`** configuration for your Spring Boot app that:

* ✅ Logs **class name**
* ✅ Logs **method name**
* ✅ Logs **line number**
* ✅ Uses clean formatting
* ✅ Separates console + file logging
* ✅ Is production-ready but simple

---

## 📄 `src/main/resources/logback-spring.xml`

```xml
<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true">

    <!-- Spring Boot default variables -->
    <springProperty scope="context" name="LOG_FILE" source="logging.file.name"/>
    <springProperty scope="context" name="LOG_PATH" source="logging.file.path"/>

    <!-- Log Pattern -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %cyan(%class{36})#%magenta(%method):%line - %msg%n"/>

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Rolling File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>logs/application.log</file>

        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover -->
            <fileNamePattern>logs/application.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>14</maxHistory>
        </rollingPolicy>

        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
        </encoder>
    </appender>

    <!-- Set logging levels -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE"/>
    </root>

    <!-- Optional: More detailed logging for your package -->
    <logger name="com.yourpackage" level="DEBUG"/>

</configuration>
```

---

## 🔎 What Makes Class + Method Appear?

This part:

```xml
%class{36}#%method:%line
```

| Pattern      | Meaning                   |
| ------------ | ------------------------- |
| `%class{36}` | Class name (max 36 chars) |
| `%method`    | Method name               |
| `%line`      | Line number               |
| `%msg`       | Log message               |

Example output:

```
2026-02-17 14:32:11.245 [http-nio-8080-exec-1] INFO  HousingService#getAveragePrice:87 - Calculating average price for LS1
```

---

## ⚠️ Performance Note (Important)

Including:

```
%class
%method
%line
```

requires stack trace inspection and **has a small performance cost**.

For production high-traffic systems, many teams remove `%line`.

For your coursework / microservice project → this is perfectly fine.

---

## 🧠 Optional: Cleaner Production Pattern

If you later want a lighter production version:

```xml
%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{36} - %msg%n
```

This removes method + line lookup.

---

If you'd like, I can also give you:

* 🔐 A JSON logging config (for ELK stack)
* 📦 A profile-based config (dev vs prod)
* 🚀 A version optimised for Kubernetes

Just tell me your deployment setup.
