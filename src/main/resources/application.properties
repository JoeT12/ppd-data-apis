spring.application.name=Coursework-1

# H2 database chosen for local integration due to simplicity.
# This will not scale beyond local dev, however - as it creates a file.
# Postgres SQL would be a better choice for non-dev environments.

spring.datasource.url=jdbc:h2:file:./data/db
spring.datasource.username=admin
spring.datasource.password=Q7!mA9#Zr2$

# Flyway will create the database automatically (if it doesn't exist) and
# perform migrations on application start.
flyway.enabled=true
